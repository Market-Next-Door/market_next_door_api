# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/configuration-reference/#jobs
jobs:
  say-hello:
    # Specify the execution environment. You can specify an image from Docker Hub or use one of our convenience images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/configuration-reference/#executor-job
    docker:
      - image: cimg/base:stable
    # Add steps to the job
    # See: https://circleci.com/docs/configuration-reference/#steps

    environment:
      DATABASE_URL: postgres://vluxnhwlvcgwpb:0d4fe0a86cebef300988f11ffb19720ad9683d46391275f52f76fd427c84bffa@ec2-54-211-177-159.compute-1.amazonaws.com:5432/d7qmstemeigg3j

    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
  
  run-tests:
    docker:
      - image: circleci/python:3.10
    steps:
      - checkout
      - run:
          name: "Install requirements"
          command: |
            python -m venv .venv
            . .venv/bin/activate
            /home/circleci/project/.venv/bin/python -m pip install --upgrade pip
            pip install -r requirements.txt

      - run:
          name: "Run tests"
          command: |
            . .venv/bin/activate
            python3 manage.py test market_next_door_api.tests

# Orchestrate jobs using workflows
# See: https://circleci.com/docs/configuration-reference/#workflows
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
      - run-tests
